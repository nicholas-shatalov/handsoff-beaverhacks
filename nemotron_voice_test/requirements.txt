# NVIDIA NIM streaming ASR dependencies
torch>=2.0.0
soundfile>=0.12.0
numpy>=1.24.0
pyaudio>=0.2.13
websockets>=12.0
 
# For batch transcription with main.py (optional if using NIM API only)
# nemo-toolkit[asr] must be installed from GitHub, not PyPI:
# pip install git+https://github.com/NVIDIA/NeMo.git@main#egg=nemo_toolkit[asr
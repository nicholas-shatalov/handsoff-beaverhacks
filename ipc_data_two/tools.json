[
    {
        "type": "task",
        "function": {
            "name": "open_application",
            "description": "Opens a specified app",
            "parameters": {
                "type": "object",
                "properties": {
                    "app_name": {"type": "string", "description": "Name of application"},
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["app_name", "agent"]
            }
        }   
    },
    {
        "type": "task",
        "function": {
            "name": "open_url",
            "description": "Opens a URL in the browser",
            "parameters": {
                "type": "object",
                "properties": {
                    "url": {"type": "string", "description": "URL to open"},
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["url", "agent"]
            }
        }
    },
    {
        "type": "task",
        "function": {
            "name": "search_browser",
            "description": "Opens the browser and searches searchterms",
            "parameters": {
                "type": "object",
                "properties": {
                    "searchterm": {"type": "string", "description": "Term to search"},
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["searchterm", "agent"]
            }
        }
    },
    {
        "type": "task",
        "function": {
            "name": "open_youtube",
            "description": "Opens YouTube",
            "parameters": {
                "type": "object",
                "properties": {
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["agent"]
            }
        }
    },
    {
        "type": "task",
        "function": {
            "name": "click",
            "description": "Clicks at a location description",
            "parameters": {
                "type": "object",
                "properties": {
                    "location": {"type": "string", "description": "A description of the location where to click"},
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["location", "agent"]
            }
        }
    },
    {
        "type": "task",
        "function": {
            "name": "type_text",
            "description": "Types text to the computer",
            "parameters": {
                "type": "object",
                "properties": {
                    "text": {"type": "string", "description": "Text being typed"},
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["text", "agent"]
            }
        }
    },
    {
        "type": "task",
        "function": {
            "name": "press_key",
            "description": "Inputs a key to the computer",
            "parameters": {
                "type": "object",
                "properties": {
                    "key": {"type": "string", "description": "Name of key pressed"},
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["key", "agent"]
            }
        }
    },
    {
        "type": "task",
        "function": {
            "name": "focus_window",
            "description": "Brings an open window into focus",
            "parameters": {
                "type": "object",
                "properties": {
                    "title_substring": {"type": "string", "description": "Name of the window to be focused"},
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["title_substring", "agent"]
            }
        }
    },
    {
        "type": "task",
        "function": {
            "name": "scroll",
            "description": "Scrolls in the application",
            "parameters": {
                "type": "object",
                "properties": {
                    "direction": {"type": "string", "description": "Has to be up or down"},
                    "amount": {"type": "integer", "description": "The amount of scroll"},
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["direction", "amount", "agent"]
            }
        }
    },
    {
        "type": "task",
        "function": {
            "name": "zoom",
            "description": "Zoom in on the application or window",
            "parameters": {
                "type": "object",
                "properties": {
                    "direction": {"type": "string", "description": "Has to be in or out"},
                    "amount": {"type": "integer", "description": "The amount to zoom"},
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["direction", "amount", "agent"]
            }
        }
    },
    {
        "type": "task",
        "function": {
            "name": "take_screenshot",
            "description": "Take a screenshot of the window",
            "parameters": {
                "type": "object",
                "properties": {
                    "path": {"type": "string", "description": "Place for screenshot to be stored, if no path is specified store the screenshot in the C drive path"},
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["path", "agent"]
            }
        }
    },
    {
        "type": "task",
        "function": {
            "name": "search_on_youtube",
            "description": "Search for a video on YouTube",
            "parameters": {
                "type": "object",
                "properties": {
                    "query": {"type": "string", "description": "Name of video to search"},
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["query", "agent"]
            }
        }
    },
    {
        "type": "task",
        "function": {
            "name": "generate_text",
            "description": "Generates text based on the user prompt",
            "parameters": {
                "type": "object",
                "properties": {
                    "text_prompt": {"type": "string", "description": "Description of the text the user wants generate"},
                    "agent": {
                        "type": "string",
                        "description": "The agent assigned to this task",
                        "enum": ["GUI", "Writing", "Actions"]
                    }
                },
                "required": ["text_prompt", "agent"]
            }
        }
    }
]